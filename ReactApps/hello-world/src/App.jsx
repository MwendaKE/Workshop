// We import React so we can use JSX
import React from "react";

// This is our App component
// A component is just a function that returns JSX (HTML-like syntax)
function App() {
  return (
    <div>
      {/* This is what will appear on the screen */}
      <h1>Hello, World!</h1>
    </div>
  );
}

// We export App so Vite can show it in the browser
export default App;

