🧩 Key Improvements
=====================
SQLite DB: Tasks now persist in a structured way.
Database table: tasks table stores an id and task text.
CRUD operations: Add and remove tasks from DB + UI.
Scalable: Later one can add “completed” status, due dates, or priorities.

🧰 Compile Command
===================
moc mainwindow.h -o mainwindow.moc.cpp
g++ -fPIC main.cpp mainwindow.cpp mainwindow.moc.cpp -o todo `pkg-config --cflags --libs Qt5Widgets Qt5Sql`

Run:
----
./todo


💡 Key takeaways:
=================
Every Qt class with Q_OBJECT → must run moc.
Either manually generate .moc.cpp or let qmake/CMake do it.
Once moc is included in compilation, the vtable errors disappear.
