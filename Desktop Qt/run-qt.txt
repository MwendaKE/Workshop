🧰 Step 1. Make sure Qt5 and compiler are installed
===================================================

sudo apt update
sudo apt install build-essential qtbase5-dev qt5-qmake
g++ --version
qmake --version

🧩 Step 2. Install required VS Code extensions
==============================================
Open VS Code → go to Extensions (Ctrl+Shift+X) and install:

C/C++ (by Microsoft) → for IntelliSense and debugging
Code Runner (optional, to run small programs easily)
Qt Tools (optional, helps with Qt syntax highlighting)

🏗️ Step 3. Create a new Qt project
==================================
mkdir ~/qt-vscode
cd ~/qt-vscode
code main.cpp

paste:
------
#include <QApplication>
#include <QPushButton>

int main(int argc, char *argv[]) {
    QApplication app(argc, argv);
    QPushButton button("Hello Qt5 from VS Code!");
    button.resize(250, 70);
    button.show();
    return app.exec();
}

⚙️ Step 4. Create a tasks.json file to build your app
=====================================================
In VS Code:
----------
Press Ctrl+Shift+P → type “Tasks: Configure Task” → select it
Choose Create tasks.json file from template → Others

Replace the content with this:
------------------------------
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build-qt",
            "type": "shell",
            "command": "g++",
            "args": [
                "main.cpp",
                "-o", "app",
                "`pkg-config", "--cflags", "--libs", "Qt5Widgets`"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"]
        }
    ]
}

📝 Note: the backticks (`) tell the shell to use pkg-config to automatically include Qt’s include paths and libraries.
---------

▶️ Step 5. Build the program
============================
Press Ctrl+Shift+B → select build-qt.
This compiles your program into a file called app.


🚀 Step 6. Run the program
==========================
Open the terminal inside VS Code (Ctrl+`) and run:

./app

A small window should appear saying:
“Hello Qt5 from VS Code!”

💡 Optional: Debug setup
=========================
You can also set up debugging with launch.json.


%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
fPIC (Position Independent Code).
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
✅ Step 1. Run this command instead
===================================
----
g++ -fPIC main.cpp -o app `pkg-config --cflags --libs Qt5Widgets`
______
The -fPIC flag is the key — it tells g++ to generate position-independent code compatible with how Qt5 is built.


🧠 Step 2. What that does
=========================
- g++ → the C++ compiler

- fPIC → fixes the relocation issue

`pkg-config --cflags --libs Qt5Widgets` → automatically includes Qt5’s headers and libraries
\

🚀 Step 3. Run your program
============================
After it compiles without error, run:

./app


⚙️ Step 4. (Optional) Fix it permanently in VS Code
===================================================
Open .vscode/tasks.json and add -fPIC in the build command like this:

"args": [
    "-fPIC",
    "main.cpp",
    "-o", "app",
    "`pkg-config", "--cflags", "--libs", "Qt5Widgets`"
]

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
To make VS Code run your Qt app automatically after building (so you don’t have to type ./app manually).

🧩 Step 1. Open your project’s .vscode/tasks.json
==================================================
If you don’t have it yet:

In VS Code, press Ctrl + Shift + P
Type: Tasks: Configure Task
Choose Create tasks.json file from template → Others

🛠 Step 2. Replace its content with this
=========================================
{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run Qt app",
            "type": "shell",
            "command": "g++",
            "args": [
                "-fPIC",
                "main.cpp",
                "-o", "app",
                "`pkg-config", "--cflags", "--libs", "Qt5Widgets`"
            ],
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "detail": "Build the Qt application",
            "dependsOn": ["run Qt app"]
        },
        {
            "label": "run Qt app",
            "type": "shell",
            "command": "./app",
            "problemMatcher": []
        }
    ]
}

✅ Explanation:
---------------
The first task compiles your code (build and run Qt app)
After it builds, it automatically triggers the second task (run Qt app)
So you only press Ctrl + Shift + B — and it compiles and runs your Qt window instantly

▶️ Step 3. Use it
==================
Ctrl + Shift + B

Select:
👉 build and run Qt app

Wait a few seconds — your Qt window will pop up automatically! 🎉

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
Run app from certain directory
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
✅ Step 1. Find the full path of your project folder
===================================================
$ pwd
/home/mwenda/Desktop/CES/Workshop/Desktop Qt

Copy that entire path.

⚙️ Step 2. Edit your .vscode/tasks.json
=======================================
Open it and make it look like this (replace with your real path):

{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "build and run Qt app",
            "type": "shell",
            "command": "g++",
            "args": [
                "-fPIC",
                "main.cpp",
                "-o", "app",
                "`pkg-config", "--cflags", "--libs", "Qt5Widgets`"
            ],
            "options": {
                "cwd": "/home/mwenda/Desktop/CES/Workshop/Desktop Qt"
            },
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": ["$gcc"],
            "dependsOn": ["run Qt app"]
        },
        {
            "label": "run Qt app",
            "type": "shell",
            "command": "./app",
            "options": {
                "cwd": "/home/mwenda/Desktop/CES/Workshop/Desktop Qt"
            },
            "problemMatcher": []
        }
    ]
}

🔑 Important:
-------------
The cwd key sets the current working directory for the command.
Make sure the path matches exactly where your main.cpp and app are.

▶️ Step 3. Build and run again
===============================
Now press:

Ctrl + Shift + B

Select “build and run Qt app”

✅ It should:
-------------
Compile main.cpp
Create app in that folder
Run it automatically — showing your Qt window