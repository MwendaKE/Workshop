“Simple Address Book”.
=====================
This project will teach you:

Using QTableWidget for tabular data.
Managing multiple fields per item (name, phone, email, address).
Signals and slots for buttons like Add, Edit, Delete, and Search.
Optionally storing data in SQLite for persistence (like we did with the to-do list).
Organizing code in multi-file format.

💡 Project Concept
==================
Features:

Display all contacts in a table.
Add new contacts (Name, Phone, Email, Address).
Edit selected contacts.
Delete selected contacts.
Search contacts by name or phone.
Optional: Save/load contacts from SQLite database.

AddressBook/
├── main.cpp
├── mainwindow.h
├── mainwindow.cpp
├── contact.h        // Optional: Define a Contact struct/class


⚡ Why this project?
====================
It introduces tabular views with QTableWidget.
Teaches handling multiple fields per row.
Gives experience with signals & slots for CRUD operations.
Lays the foundation for more advanced apps like inventory systems or CRM apps.
